import {Div} from "../atoms/index.js"

export const cartListView = () => {
    const element = Div()
    element.innerText = "Indkøbskurv"

    return element
}  const productId = form.productId.value